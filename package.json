{
  "name": "uni-prototype",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node deploy.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}

